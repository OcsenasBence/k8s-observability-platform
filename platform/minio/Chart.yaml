apiVersion: v2
name: minio-config
description: Minio S3 storage for Mimir
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: minio
    version: "*"
    repository: https://charts.min.io/